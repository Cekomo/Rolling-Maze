using System.Collections.Generic;
using UnityEngine;

// this class can be converted into struct or else regarding current need
public class MazeModels : MonoBehaviour
{
    public List<GameObject> mazeModels;
}
